program
	func (a:Integer, b:Boolean): Boolean is
		begin
			b:=(not b);
			print (a >= 7);
			return a;
		end;
begin
	func(2, true);
end;

However, weird stuff like this <===x should
be okay, even though it will fail during parsing